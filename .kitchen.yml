---
provisioner:
  name: terraform

verifier:
  name: terraform
  systems:
    - name: basic
      backend: local

platforms:
  - name: terraform

suites:
  - name: terraform-gcp-project
    driver:
      name: terraform
      root_module_directory: examples
      parallelism: 4
      command_timeout: 1800
